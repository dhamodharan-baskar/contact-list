import {
  
} from '../list.styles'

const Header = (props) => {
  return (
    <div>
      Contact Manager 
      <div>
       No of Contacts
       No of Countries 
      </div>
      Sort by
      Filter by
    </div>
  )
};

export default Header;
